#!/usr/bin/env bash

cmd="${1//\//_}"

# set XDG_ dirs to smaller number of candidates for less noisy trace
XDG_DATA_DIRS="$HOME/.local/share:/usr/share" XDG_CONFIG_DIRS="/etc/xdg" strace -s 1024 -f -o "tmp/${cmd}-strace" "$@"

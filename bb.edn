 {:tasks {test
          {:doc  "runs tests"
           :task (clojure {:dir "."} "-M:test")}

          test-watch
          {:doc  "runs tests in watch mode"
           :task (clojure {:dir "."} "-M:test --watch")}}}
